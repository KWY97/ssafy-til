<README>
# matplotlib.pyplot로 그래프 생성
# plt.show() 하면 새 창을 띄워주는 것이지 홈페이지로 넘겨주지 않음

# 홈페이지로 넘길 방식이 필요
    # 1. 그려진 객체를 반환받아 넘기기 -> 가능여부: X, matplotlib이 지원하지 않음
    # 2. 이미지로 저장하기 -> 가능여부: O, 간단하지만, 용량이 감당안됨
    # 3. 버퍼(임시 저장 공간)를 활용 -> 가능여부: O, 우리가 활용할 방법
        # 'BytesIO' 클래스 활용
            # 파이썬의 내장 모듈인 'io' 모듈에 포함된 클래스
            # 메모리 내에 데이터를 저장 및 조작할 수 있는 기능 제공
            # io: 입출력 연산을 위한 Python 표준 라이브러리
            # ByteIO: 메모리 내에서 이진 데이터를 파일처럼 다룰 수 있는 버퍼를 제공

-> 그냥 views.py 보고 코드 흐름 맞춰서 주석들 README에 적어도 될 듯


*) PJT02 정리코드 한 번 보고 프로젝트 진행하면 도움될 것이다.
*) Django 할 때 프로젝트, 앱의 이름은 최대한 변경하지 말고, 다시 삭제하고 새로 만드는 것이 나음

!! MTV 흐름대로 잘 생각하기 !! 
URL -> VIEW -> TEMPLATE

