<장고 인트로>
Web application (Web service) 개발
 - 인터넷을 통해 사용자에게 제공되는 소프트웨어 프로그램을 구축하는 과정
 - 다양한 디바이스(모바일, 태블릿, PC 등)에서 웹 브라우저를 통해 접근하고 사용할 수 있음

Client (클라이언트) - 서비스를 '요청'하는 주체 (웹 사용자의 인터넷이 연결된 장치, 웹 브라우저)
Sever (서버) - 클라이언트의 요청에 '응답'하는 주체 (웹 페이지, 앱을 저장하는 컴퓨터)
*) !!! 요청이 없으면 응답이 진행되지 않는다 !!!

우리가 웹 페이지를 보게 되는 과정
 1. 웹 브라우저(클라이언트)에서 'google.com'을 입력 후 엔터
 2. 웹 브라우저는 인터넷에 연결된 전세계 어딘가에 있는 구글 컴퓨터(서버)에게 '메인 홈페이지.html' 파일을 달라고 요청
 3. 요청을 받은 구글 컴퓨터는 데이터베이스에서 '메인 홈페이지.html' 파일을 찾아 응답
 4. 웹 브라우저는 전달받은 '메인 홈페이지.html' 파일을 사람이 볼 수 있도록 해석해주고 사용자는 구글의 메인 페이지를 보게 됨

웹 개발에서의 Frontend와 Backend
 Frontend
  - 사용자 인터페이스(UI)를 구성하고, 사용자가 애플리케이션과 상호작용할 수 있도록 함
    (HTML, CSS, JavaScript, 프론트엔드 프레임워크 등)

 Backend
  - 서버 측에서 동작하며, 클라이언트의 요청에 대한 처리와 데이터베이스와의 상호작용 등을 담당
    (서버언어 (Python, Java 등) 및 백엔드 프레임워크, 데이터베이스, API, 보안 등)
    *) Django는 Backend 부분

<Web Framework>
 - 웹 애플리케이션을 빠르게 개발할 수 있도록 도와주는 도구
   (개발에 필요한 기본 구조, 규칙, 라이브러리 등을 제공)

 !!! django - 파이썬 기반의 대표적인 웹 프레임워크 !!!
 - 왜 Django를 사용할까? (딱히 외울 필요는 없어 보이는 느낌)
  1. 다양성, 2. 확장성, 3. 보안, 4. 커뮤니티 지원

  - !!! 대규모 트래픽 서비스에서도 안정적인 서비스 제공 !!! (이게 중요)
  - 2024 가장 인기있는 Backend Framework 2위 (1위는 Express.js (Node.js) - javascript 관련된 것임)

- 우리가 앞으로 할 것: Django를 사용해서 !!'서버'!!를 구현할 것



!!! 가상환경 !!! - 중요한 내용
 Python 애플리케이션과 그에 따른 패키지들을 '격리'하여 관리할 수 있는 '독립적인' 실행 환경
 (필요한 상황 예시: 프로젝트 여러개를 진행하는데 프로젝트 별로 '패키지 버전'이 다른경우, 프로젝트 별로 사용하는 '패키지'가 다른 경우)
 
 1. 가상환경 생성
 $ python -m venv '가상환경이름' (근데 가상환경이름은 venv 라고 하는게 관례적이기 때문에 venv로 해야한다.)
 ($ python -m venv venv   이렇게 하면 댐) (*) 가상환경 생성하면 물리적으로 venv(가상환경이름) 폴더가 생긴다. 폴더안에 있는 것은 절대로 건드리면 안됨.)
 
 
 2. 가상 환경 활성화 (on 하는 법)
 $ source venv/Scripts/activate (가상환경 폴더 내 파일의 경로를 적은 것인데 - 이게 활성화 하는 방법임)
 *) !!! 가상환경은 on/off 개념이지 in/out 개념이 아니다. !!! (중요)
 *) 가상환경을 어디에서든 활성화 할 수 있음. (경로만 제대로 적어준다면)
 !! 활성화 명령어가 OS에 따라 다름을 주의해야한다.!!
 (macOS/Linux: $ source venv/bin/activate)
 *) 보통 가상환경은 그 환경을 사용하는 프로젝트와 동일한 위치에 둔다.
 *) 가상환경은 경로로 나타내기 때문에 다른 경로에서 동일한 이름으로 만들 수 있다. (각 프로젝트 폴더에서 가상환경 쓸거면 venv라는 이름로 쭉 만들면 된다.)

+) 활성화 후 장고 설치: pip install django
 
 가상환경 비활성화(off)
 $ deactivate
 (근데 사실 터미널 끄면 가상환경 비활성화되긴함. 공식으로 끄는 명령어가 있다는 정도만 알기)


 3. 환경에 설치된 패키지 목록 확인
 $ pip list


 4. 설치된 패키지 목록 생성
 $ pip freeze > requirements.txt
 : 현재 Python 환경에 설치된 모든 패키지와 그 버전을 requirements.txt 라는 파일로 저장
 (requirements.txt 는 관례적으로 사용하는 파일 이름임. 다른이름 쓰지 말기)

 패키지 목록이 필요한 이유 - 같은 프로젝트를 진행하는데 각 팀원이 그 프로젝트를 위해 어떤 패키지를 설치했고, 어떤 버전을 설치 했는지 모르기 때문에 실행이 안될 수도 있기 때문
 그렇기 때문에 패키지 목록을 공유함
 설치된 패키지 목록말고 venv를 공유하면 되지만 용량이 너무커서 패키지 목록을 만들고 그 목록을 공유하는 것임 
 *) !! gitignore에는 venv가 포함돼 있다. (venv 는 용량이 너무 크기 때문에) !!

 +) $ pip install -r requirements.txt
 : 생성된 requirements.txt에 작성된 목록을 기반으로 설치 (하나하나 설치 안해도 된다는 말)
    
 패키지 목록 파일 특징 및 주의사항
  특징
   - 가상환경의 패키지 목록을 쉽게 공유 가능
   - 프로젝트의 의존성을 명확히 문서화
   - 동일한 개발 환경을 다른 시스템에서 재현 가능
   
  주의사항
   - '활성화'된 가상환경에서 실행해야 정확한 패키지 목록 생성
   - 시스템 전역 패키지와 구분 필요

 가상환경 비활성화
  $ deactivate
  (근데 사실 터미널 끄면 가상환경 비활성화되긴함. 공식으로 끄는 명령어가 있다는 정도만 알기)

Django 프로젝트 생성 및 서버 실행
 1. 프로젝트 생성 (firstpjt라는 이름의 프로젝트를 생성, '.'은 현재 폴더에 만들겠다는 뜻임 '.'을 안하면 폴더가 하나 더 생기고 그 폴더에 firstpjt 만들어짐)
   $ django-admin startpoint firstpjt .

 2. 서버 실행
   $ python manage.py runserver


디자인 패턴
 - 소프트웨어 설계에서 발생하는 문제를 해결하기 위한 일반적인 해결책
   (공통적인 문제를 해결하는데 쓰이는 형식화된 관행)
   ("애플리케이션의 구조는 이렇게 구성하자"라는 관행)

 - 이 관행이 발전하면서 효율적인 구조를 찾게 됐음 그것이 바로 MCV 디자인 패턴

MCV 디자인 패턴 (Model, View, Controller)
 - 애플리케이션을 구조화하는 대표적인 패턴 
   ("데이터" & "사용자 인터페이스" & "비즈니스 로직"을 분리)
   시각적 요소와 뒤에서 실행되는 로직을 서로 영향 없이, 독립적이고 '쉽게 유지 보수'할 수 있는 애플리케이션을 만들기 위해

MTV 디자인 패턴 (Model, Template, View)
 - Django에서 애플리케이션을 구조화 하는 패턴
   (기존 MVC 패턴과 동일하나 '단순히 명칭을 다르게' 정의한 것)

 Model - 데이터와 관련된 로직을 관리, 응용프로그램의 데이터 구조를 정의하고 데이터베이스의 기록을 관리
 Template - 레이아웃과 화면을 처리, 화면상의 사용자 인터페이스 구조와 레이아웃을 정의
 View - Model & Template과 관련한 로직을 처리해서 응답을 반환, 클라이언트의 요청에 대해 처리를 분기하는 역할

 *) View 예시
    데이터가 필요하다면 model에 접근해서 데이터를 가져오고,
    가져온 데이터를 template으로 보내 화면을 구성하고,
    구성된 하면을 응답으로 만들어 클라이언트에게 반환

Project & App
 Django project
  - 애플리케이션의 집합 (DB 설정, URL 연결, 전체 앱 설정 등을 처리)
 
 Django apllication
  - 독립적으로 작동하는 '기능 단위' 모듈 (각자 특정한 기능을 담당하며 다른 앱들과 함께 하나의 프로젝트를 구성)

 앱을 사용하기 위한 순서
  1. 앱  생성
   $ python manage.py startapp articles
   (articles는 앱의 이름. 앱의 이름은 복수형으로 지정하는 것을 권장!)
   (*) articles가 프로젝트 안에 생기는 것이 아님. !! 그렇기 때문에 프로젝트에 등록해야하는 것)

  2. 앱 등록 - 반드시 !! 앱을 생성한 후!! 에 등록 해야함 (등록 후 생성은 불가능)
  프로젝트의 settings.py 파일에서 INSTALLED_APPS 라는 리스트에 앱 이름을 넣어줘야함
  이미 작성돼 있는 것은 내장앱임.
  그렇기 때문에 내장앱보다 위에 우리가 작성한 앱(커스텀 앱)을 적어줘야함 ( 권장사항임. 내장앱은 순서적으로 뒤에 구동 되는 것이 안정적이라고 함)

프로젝트 구조 (프로젝트 파일의 구조)
 - setting.py (중요) !!
  프로젝트의 모든 설정을 관리

 - urls.py (중요) !!
  요청 들어오는 URL에 따라 이에 해당하는 적절한 views를 연결

 - __init__.py (별로 안중요, 수업 때 건들지도 않음)
  해당 폴더를 패키지로 인식하도록 설정하는 파일

 - asgi.py (별로 안중요, 수업 때 설정 따로 할 것 없음)
  비동기식 웹 서버와의 연결 관련 설정

 - wsgi.py (별로 안중요)
  웹 서버와의 연결 관련 설정

 - manage.py (중요한데 우리 수업할 땐 터치 안함)
  Django 프로젝트와 다양한 방법으로 상호작용 하는 커맨드라인 유틸리티


앱 구조
 - admin.py (중요)
  관리자용 페이지 설정
 
 - models. py (중요)
  DB와 관련된 Model을 정의
  MTV 패턴의 M

 - views.py (가장 중요)
  HTTP 요청을 처리하고 해당 요청에 대한 '응답'을 반환
  (url, model, tempalte과 연계)
  MTV 패턴의 V

 - apps.py (별로 안중요, 수업 때 안건드림)
  앱의 정보가 작성된 곳

 - test.py (별로 안중요, 수업 때 안건드림)
  프로젝트 테스트 코드를 작성하는 곳

 
Django와 요청 & 응답 - 77, 78p 보면 댄다 
 urls.py -> views.py -> templates 순서 잘 기억하기 (지금은 models.py 부분은 생각 안하는 것)

 URLs
  request 객체를 views 모듈의 index view 함수에게 전달하며 호출
  url 경로는 반드시 '/'로 끝나야함
  from articles import views (articels 패키지에서 views 모듈을 가져오는 것)

 View
  view 함수가 정의 되는 곳
  특정 경로에 있는 template과 request 객체를 결합해 응답 객체를 반환
  *) 모든 view함수는 첫번째 인자로 요청 객체를 필수적으로 받음
     매개변수 이름이 request가 아니어도 되지만 그렇게 작성하지 않음

 Template
  artilces 앱 폴더 안에 templates 폴더 생성 (폴더명은 반드시 templates 이어야 한다.)
  !!! Django에서 template을 인식하는 경로 규칙 !!!
  app폴더 / templates/  이 두개는 기본 경로로 인식 그 뒤에 이제 파일의 위치 경로 적으면 댐

 !! 요청과 응답 과정 정리 !! - 처음부터 정말 잘 이해해놓아야함 (교쟈 86p)
 1. 브라우저가 http://127.0.0.1:8000/index/로 요청 (urls.py가 받는다)
 2. index/ 주소와 일치하는 view 함수 호출
 3. index view 함수가 응답 객체 생성 및 반환
 4. 브라우저는 index view 함수가 반환한 응답 객체를 해석해 페이지를 렌더링
 *) URLs -> Veiw -> Template 순서!!


참고)

git 쓸 때 순서
 1. 가상환경 생성
 2. 가상환경 활성화
 3. Django 설치
 4. 패키지 목록 파일 생성
 5. !!! .gitignore 파일 생성 (첫 add 전) !!!
 6. !!! git 저장소 생성 (git init) !!!
 7. Django 프로젝트 생성


LTS (Long-Term Support)
 프레임워크나 라이브러리 등의 소프트웨어에서 장기간 지원되는 안정적인 버전을 의미
 장고는 4.2 !!! 

render 함수
 주어진 템플릿을 주어진 컨텍스트 데이터와 겨합하고 렌더링된 텍스트와 함께 HttpResponse !!응답 객체!!를 반환하는 함수
 인자는 request, template_name, context 오는데 context는 오늘 다루지 않았음.
 request - 응답을 생성하는데 사용되는 요청 객체
 template_name - 템플릿 이름의 경로
